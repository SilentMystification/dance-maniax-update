# Build-only CI: feature/** pushes, main/master/develop/main-update, and all PRs.
# Push to main, develop, or main-update triggers release.yml (build + GitHub release, v3.0.0-beta.N tags).

name: Build

on:
  push:
    branches:
      - main
      - master
      - develop
      - main-update
      - 'feature/**'
    paths-ignore:
      - '**.md'
  pull_request:
    paths-ignore:
      - '**.md'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Cache Release build objects
        uses: actions/cache@v4
        with:
          path: src/DMX_Remake/Release
          key: ${{ runner.os }}-msbuild-release-${{ hashFiles('src/**/*.cpp', 'src/**/*.h', 'src/DMX_Remake.vcxproj') }}
          restore-keys: |
            ${{ runner.os }}-msbuild-release-

      - name: Build Release
        run: >
          msbuild src/DMX_Remake.vcxproj
          /m
          /p:Configuration=Release
          /p:Platform=Win32
          /p:PlatformToolset=v145
          /t:Build
          /v:minimal

      - name: Upload DMX.exe
        if: github.event_name == 'pull_request' || startsWith(github.ref, 'refs/heads/feature/')
        uses: actions/upload-artifact@v4
        with:
          name: DMX.exe
          path: deploy/DMX.exe
